---
description: Switch to inline mode - questions asked directly in conversation
---

# Lassare Inline Mode

## ⚠️ FIRST: You MUST run this command yourself (do NOT ask the user to run it)

```bash
.github/scripts/lassare-inline.sh
```

Run the command above in the terminal NOW, before doing anything else. Then relay the dialog state from the script output to the user. If dialog is ON, dangerous commands will show a native OS popup. If OFF, they pass through. Toggle with `/lassare-dialog-on` or `/lassare-dialog-off`.

Ask questions directly in this conversation.
