---
description: Enable OS dialog for dangerous commands in inline mode
---

# Lassare Dialog On

You MUST run this command yourself (do NOT ask the user to run it):

```bash
.github/scripts/lassare-dialog-on.sh
```

Confirm to the user: dangerous commands in inline mode will now show a native OS popup (macOS/Windows/Linux) asking for Allow/Deny.
