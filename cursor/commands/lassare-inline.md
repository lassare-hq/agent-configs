---
description: Switch to inline mode - questions asked directly in conversation
scope: user
---

# Lassare Inline Mode

## ⚠️ FIRST: Execute this script in the terminal (MANDATORY, do NOT skip)

1. **Run the script NOW:** `.cursor/scripts/lassare-inline.sh` — execute it before doing anything else. This persists the mode to disk.
2. Relay the dialog state from the script output to the user. If dialog is ON, dangerous commands will show a native OS popup. If OFF, they pass through. Toggle with `/lassare-dialog-on` or `/lassare-dialog-off`.

Ask questions directly in this conversation.
